AWS Architecture Diagram — Perspective (Data Discovery Solution)
================================================================

HIERARCHY
---------
AWS Cloud (outermost)
├── Web UI component (top-left, dashed)
│   ├── Amazon DynamoDB Settings table
│   ├── Amazon Cognito
│   ├── Amazon CloudFront
│   └── Amazon S3 bucket WebUIBucket
├── AWS Lambda Settings function (standalone, right of Web UI)
├── Cost component (top-right, dashed)
│   ├── AWS Lambda Cost function
│   ├── Amazon Athena
│   ├── Amazon S3 bucket CURBucket
│   ├── AWS Cost & Usage Report (CUR)
│   └── Amazon S3 bucket AthenaResultsBucket
├── Client API (gray box, center)
│   ├── Amazon API Gateway
│   └── AWS AppSync
├── VPC (green solid border)
│   └── Private subnet (blue dashed)
│       ├── Data component (dashed)
│       │   ├── AWS Lambda Gremlin function
│       │   ├── Amazon Neptune
│       │   ├── AWS Lambda Search function
│       │   └── Amazon OpenSearch Service
│       └── Discovery component (dashed)
│           ├── AWS Fargate
│           ├── Amazon Elastic Container Service
│           └── Amazon Elastic Container Registry
├── Image deployment component (right side, dashed)
│   ├── Amazon S3 bucket DiscoveryBucket
│   ├── AWS CodePipeline
│   ├── AWS CodeBuild
│   └── Container image
├── Storage management component (bottom-left, dashed)
│   ├── AWS Amplify
│   └── Amazon S3 bucket AmplifyStorageBucket
├── Amazon API Gateway ServiceGremlin API (standalone)
├── AWS SDK (standalone)
└── AWS Config (standalone)

EXTERNAL
--------
Users (people icon, outside AWS Cloud, left side)

NUMBERED CIRCLES (1-17)
------------------------
1  — on arrow Users → CloudFront
2  — between CloudFront and S3 WebUIBucket
3  — between Amplify and S3 AmplifyStorageBucket
4  — on API Gateway (green)
5  — on arrow from Client API up to Cognito
6  — between DynamoDB and Lambda Settings
7  — between Lambda Gremlin and Neptune
8  — between Lambda Search and OpenSearch
9  — between Lambda Cost and Athena
10 — between Athena and S3 CURBucket
11 — between CUR and S3 AthenaResultsBucket (or near CUR)
12 — near Lambda Cost function
13 — on CodePipeline
14 — near ECR
15 — near ECS
16 — near AWS SDK
17 — near API Gateway ServiceGremlin API

ARROWS / CONNECTIONS
--------------------
A1:  Users → CloudFront (horizontal right)
A2:  CloudFront → S3 WebUIBucket (horizontal right)
A3:  S3 WebUIBucket → Cognito (vertical up, from S3 area to Cognito)
A4:  Cognito ← Client API area (arrow with 5, going left from Client API to Cognito)
A5:  DynamoDB ← Lambda Settings (horizontal left arrow, with 6)
A6:  Client API ↕ VPC (vertical double-headed arrow down)
A7:  Client API → API GW Gremlin API (horizontal or L-shape right-down)
A8:  API GW Gremlin API → VPC Data component (arrow into VPC)
A9:  Lambda Gremlin → Neptune (horizontal right, with 7)
A10: Lambda Search → OpenSearch (horizontal right, with 8)
A11: Lambda Cost → Athena (horizontal right, with 9)
A12: Athena → S3 CURBucket (horizontal right, with 10)
A13: CUR → S3 AthenaResultsBucket (or arrow between, with 11)
A14: ECR ← Image deployment side (arrow from right, with 14)
A15: Amplify → S3 AmplifyStorageBucket (horizontal right, with 3)
